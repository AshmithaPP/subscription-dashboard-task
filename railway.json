{
  "rootDirectory": "CoreSystem/server",
  "build": {
    "builder": "NIXPACKS",
    "commands": [
      "npm install",
      "npm run build"
    ]
  },
  "deploy": {
    "startCommand": "node dist/server.js"
  }
}
