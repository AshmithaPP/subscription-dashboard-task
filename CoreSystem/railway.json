{
  "build": {
    "builder": "NIXPACKS",
    "rootDirectory": "server",
    "commands": [
      "npm install",
      "npm run build"
    ]
  },
  "deploy": {
    "startCommand": "node dist/server.js"
  }
}
