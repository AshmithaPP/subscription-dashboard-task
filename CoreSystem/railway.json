{
  "build": {
    "builder": "NIXPACKS",
    "rootDirectory": "administration/CoreSystem/server",
    "commands": [
      "npm install",
      "npm run build"
    ]
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
